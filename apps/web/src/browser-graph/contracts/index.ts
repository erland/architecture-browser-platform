/**
 * Strict ownership entrypoint for shared browser graph pipeline contracts.
 *
 * Prefer importing cross-stage graph pipeline types from here instead of
 * `browser-session/types`.
 */

export * from './browser-graph';
