/**
 * Domain model for saved-canvas documents.
 *
 * This layer owns the persisted document structure, sync metadata shape, and
 * pure constructors for building or cloning saved-canvas documents.
 */
export * from '../model/document';
